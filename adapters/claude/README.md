# Adapter Claude (skeleton)

Resolve um `workflow_id` para a origem atual em `.claude/`, por exemplo `copy.social` para `.claude/commands/copy-social.md`. A resolução é somente descritiva (`executable: false`); não chama `Skill`, `Agent`, `Task`, MCP ou `/schedule`.
